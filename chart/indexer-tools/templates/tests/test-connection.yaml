apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "indexer-tools.fullname" . }}-test-connection"
  labels:
    {{- include "indexer-tools.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "indexer-tools.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
